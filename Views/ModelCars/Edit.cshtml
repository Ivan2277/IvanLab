@model CarsLab.ModelCar

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>ModelCar</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="ModelName" class="control-label"></label>
                <input asp-for="ModelName" class="form-control" />
                <span asp-validation-for="ModelName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="IdEngine" class="control-label"></label>
                <select asp-for="IdEngine" class="form-control" asp-items="ViewBag.IdEngine"></select>
                <span asp-validation-for="IdEngine" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="IdBody" class="control-label"></label>
                <select asp-for="IdBody" class="form-control" asp-items="ViewBag.IdBody"></select>
                <span asp-validation-for="IdBody" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="IdPrice" class="control-label"></label>
                <select asp-for="IdPrice" class="form-control" asp-items="ViewBag.IdPrice"></select>
                <span asp-validation-for="IdPrice" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
