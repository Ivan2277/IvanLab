@model CarsLab.ModelCarYear

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>ModelCarYear</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="IdCar" class="control-label"></label>
                <select asp-for="IdCar" class ="form-control" asp-items="ViewBag.IdCar"></select>
            </div>
            <div class="form-group">
                <label asp-for="IdYear" class="control-label"></label>
                <select asp-for="IdYear" class ="form-control" asp-items="ViewBag.IdYear"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
